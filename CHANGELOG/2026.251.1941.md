# 2026.251.1941 — gate: quote the ensure-charly step name (unquoted ': ' broke YAML/actionlint)

## Summary

PR #1's squash merged before this one-line fix: the step name `Ensure charly (self-hosted image: no-op)` contains ': ' and breaks YAML parsing — actionlint flags it (syntax-check) and main's ci lint step fails. This quotes the name.

## How tested

```
$ /tmp/actionlint .github/workflows/*.yml
ACTIONLINT_OK
```

The live ci check on this PR runs the same lint (the `workflow lint` step).

## Rulebook compliance

- R1: failure RCA'd to the unquoted YAML scalar (': ' in a plain scalar); one-line fix in-tree.
- PR-only landing via the org validator.

## Change classification

- Change class: CI/hook (one line).
- Verification gate: local actionlint (pasted) + the live ci check on this PR.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*